<%= form_with(model: typeofuser, local: true) do |form| %>
  <% if typeofuser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(typeofuser.errors.count, "error") %> prohibited this typeofuser from being saved:</h2>

      <ul>
      <% typeofuser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "Nombre" %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
